#include <stdio.h>

int main() {
    int number = 15;
    if (number > 10) {
        printf("%d is greater than 10\n", number);
    } else {
        printf("%d is not greater than 10\n", number);
    }
    return 0;
}